@model IEnumerable<PBL3.Models.Product>
@if (Model != null && Model.Any())
{

    foreach (var item in Model)
    {
        <div class="product-item filter-@item.CatID">
            <div class="product discount product_filter">
                <div class="product_image">
                    <img src="@item.ListImages" alt="@item.Title" />
                </div>
                <div class="favorite favorite_left"></div>
                <div class="product_bubble product_bubble_right product_bubble_red d-flex flex-column align-items-center"><span>-$20</span></div>
                <div class="product_info">
                    <h6 class="product_name"><a href="single.html">@item.Title</a></h6>
                    @if (item.PromotionPrice > 0)
                    {
                        <div class="product_price">@item.Price<span>@item.PromotionPrice</span></div>
                    }
                    else
                    {
                        <div class="product_price">@item.Price</div>
                    }
                </div>
            </div>
            <div class="red_button add_to_cart_button"><a href="#">add to cart</a></div>
        </div>
    }
}